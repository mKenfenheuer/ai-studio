# Shell scripts run on Linux hosts. A CRLF working copy reaches them
# through scp or a bind mount and fails at the shebang line with
# "bash: No such file or directory", which is a confusing way to
# learn about line endings. Kept LF everywhere, checkout included.
*.sh text eol=lf
Dockerfile* text eol=lf
*.yml text eol=lf
